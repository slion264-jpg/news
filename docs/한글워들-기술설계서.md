# 한글 워들(자모 워들) 구현 방안 — 기술 설계서

> 2글자 한글 단어를 **자모 단위 6회 추측**으로 맞히는 워들. 단일 HTML MVP 동봉.
> 작성일: 2026-08-06 · **v2 — 단어풀 83,091개로 확장**

---

## 🔍 Step 1. 서비스 분석

### 영어 워들이 성립하는 조건
영어 워들은 세 가지 전제 위에서 작동합니다.

| 전제 | 영어 | 한글에 그대로 적용하면 |
|---|---|---|
| 한 칸 = 한 기호 | 알파벳 26자 | 음절 11,172자 → **경우의 수 폭발** |
| 기호 집합이 작음 | 26 | 음절 단위면 힌트가 거의 정보를 못 줌 |
| 위치가 선형적 | 좌→우 | 한글은 초·중·종성이 **2차원 배치** |

### 결론 — 한 칸은 "자모"여야 한다
'사과'를 두 칸으로 두면 첫 칸 후보가 수천 개라 노란색 힌트가 무의미해집니다.
**자모로 분해하면 기호 집합이 28개로 줄어** 영어 워들과 거의 같은 난이도 곡선이 나옵니다.

```
사과 → ㅅ ㅏ ㄱ ㅗ ㅏ   (5칸)
```

### MVP 범위

| 구분 | 내용 |
|---|---|
| **MVP** | 자모 분해·조합 엔진, 판정, 6회 추측, 자모 키보드, 사전 검사, 결과 공유, 통계, 다크모드 |
| **2차** | 서버 기반 일일 단어, 계정·랭킹, 하드모드 랭크전, 단어 난이도 티어, PWA 오프라인 |

---

## 🛠 Step 2. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트 | **Vanilla JS + 단일 HTML** | 게임 로직이 전부 클라이언트. 프레임워크 오버헤드가 순손해 |
| 스타일 | **순수 CSS (CDN 없음)** | 파일 하나로 오프라인 실행. 지하철에서도 켜짐 |
| 폰트 | Noto Sans KR + 시스템 폰트 폴백 | 자모 낱글자(ㅄ, ㅢ)가 폰트에 따라 깨지는 것 방지 |
| 백엔드(2차) | **Supabase** | 일일 단어·통계·랭킹만 필요 → BaaS로 충분 |
| 배포 | Vercel / GitHub Pages | 정적 파일 하나라 CDN 배포로 끝 |
| 단어 데이터 | **mecab-ko-dic + wordfreq** | 사전 실재성(품사)과 일상 빈도를 각각 다른 소스로 검증 |

> ⚠️ **법적 유의사항**: 게임 규칙(메커니즘)은 저작권 대상이 아니지만 **"Wordle"은 NYT의 등록 상표**입니다.
> 서비스명·로고·도메인에 Wordle을 쓰지 말고 독자 명칭(예: *자모워들*, *한글 다섯칸*)을 쓰세요.

---

## 🧩 핵심 설계 ① — 자모 모델 (이 게임의 전부)

### 후보 3안 비교

| 안 | 한 칸의 단위 | 키 개수 | 문제 |
|---|---|---|---|
| A. 호환 자모 그대로 | ㄲ, ㅘ, ㄼ을 각각 1칸 | 51 | 키보드가 비대해지고 ㄲ/ㄱ 관계의 힌트가 끊김 |
| B. **홑자모 완전 분해** ✅ | ㄲ→ㄱㄱ, ㅘ→ㅗㅏ, ㄼ→ㄹㅂ | **28** | 일부 시퀀스에 중의성 (아래에서 해결) |
| C. 초·중·종성 3칸 고정 | 항상 3칸씩 | 51 | 받침 없는 글자에 빈칸이 생겨 정보가 새어나감 |

**B안 선택 근거**: 분해 단위가 **두벌식 자판 키와 정확히 1:1**로 일치합니다.
즉 *"평소 타이핑하는 순서 그대로가 칸 순서"* 가 되어 규칙 설명이 필요 없습니다.
(`딸기` → ㄷ, ㄷ, ㅏ, ㄹ, ㄱ, ㅣ = 실제로 치는 그 순서)

### 분해 알고리즘

```js
// 유니코드 완성형: 0xAC00 + (초성 × 588) + (중성 × 28) + 종성
const code = ch.charCodeAt(0) - 0xAC00;
cho  = CHO [Math.floor(code / 588)];
jung = JUNG[Math.floor((code % 588) / 28)];
jong = JONG[code % 28];
// 이후 복합 자모를 홑자모로 전개
// ㄲㄸㅃㅆㅉ / ㄳㄵㄶㄺㄻㄼㄽㄾㄿㅀㅄ / ㅘㅙㅚㅝㅞㅟㅢ
```

### 조합(오토마타) — 1-룩어헤드 파서

입력한 자모를 실시간으로 글자로 보여주는 부분입니다. 핵심 규칙 한 줄:

> **받침은 "바로 뒤에 모음이 오지 않을 때만" 취한다.**

```
ㄱㅏㄱㅏ → 각? 아니오. 뒤에 ㅏ가 있으므로 받침 포기 → "가가"
ㄱㅏㄱㄱ → 뒤에 모음 없음 → 겹받침 ㄲ → "갂"
```

### ⚠️ 원리적 중의성과 해결책

완전 분해는 **되돌릴 수 없는 경우**가 존재합니다.

```
ㄱㅏㄱㄱㅐㅂㅅ  =  "가꺖"  또는  "각갮"   ← 둘 다 정당한 파싱
```

무작위 2음절 조합 10,268건 중 **225건(2.19%)** 에서 발생하며, 전부 *받침 + 쌍자음 초성* 패턴입니다.

**해결: 사전 역인덱스**
```js
DICT_INDEX = { "ㅌㅗㄱㄱㅣ": "토끼", ... }   // 자모키 → 단어
composeSmart(jamos) = DICT_INDEX[jamos.join('')] ?? compose(jamos)
```
어차피 제출 단어는 사전에 있어야 하므로, 사전을 먼저 조회하면 중의성이 **완전히 사라집니다**.
(입력 도중의 미완성 상태만 파서가 처리)

---

## 🎯 핵심 설계 ② — 판정 알고리즘

워들 표준 **2-pass**를 자모 배열에 그대로 적용합니다. 1-pass로 짜면 중복 자모에서 반드시 틀립니다.

```js
function judge(guess, answer) {
  const result = Array(answer.length).fill('absent');
  const pool = {};                      // 아직 매칭 안 된 정답 자모의 재고

  for (let i = 0; i < answer.length; i++)          // 1차: 자리까지 일치 → 초록
    if (guess[i] === answer[i]) result[i] = 'correct';
    else pool[answer[i]] = (pool[answer[i]] || 0) + 1;

  for (let i = 0; i < answer.length; i++) {        // 2차: 재고가 남은 만큼만 노랑
    if (result[i] === 'correct') continue;
    if (pool[guess[i]] > 0) { result[i] = 'present'; pool[guess[i]]--; }
  }
  return result;
}
```

**중복 자모 예시** — 정답 `나가`(ㄴㅏㄱㅏ), 추측 `가가`(ㄱㅏㄱㅏ)

| 칸 | 1 | 2 | 3 | 4 |
|---|---|---|---|---|
| 결과 | ⬛ | 🟩 | 🟩 | 🟩 |

첫 ㄱ이 노랑이 아닌 이유: 정답의 ㄱ은 이미 3번 칸에서 초록으로 소비되어 재고가 0.

### 키보드 색상 누적
`correct > present > absent` 우선순위로만 덮어씁니다. 한번 초록이 된 키는 절대 회색으로 되돌아가지 않습니다.

---

## 📚 핵심 설계 ③ — 단어풀 구축 파이프라인

워들의 체감 품질은 **"내가 아는 단어를 쳤는데 사전에 없다고 튕기는가"** 로 결정됩니다.
그래서 목록을 두 개로 분리하고, 각각 다른 데이터 소스로 만듭니다.

| 목록 | 크기 | 용도 | 만드는 법 |
|---|---|---|---|
| **허용 단어** | **83,091** | 추측으로 제출 가능 | mecab-ko-dic 표제어 전수 열거 |
| **정답 풀** | **5,798** | 실제 정답이 되는 단어 | 허용 단어 ∩ 빈도 상위 + 큐레이션 |

> 넓게 받아주되 정답은 아는 단어로만 — 영어 워들도 허용 12,972 / 정답 2,315로 같은 구조입니다.

### 파이프라인

```
① 음절 집합 확보     wordfreq 한국어 코퍼스 → 실제 쓰이는 음절 1,615자
② 후보 전수 조합     1,615 × 1,615 = 2,608,225쌍 → 자모 4~7개만 → 2,341,805개
③ 사전 실재 검증     mecab-ko-dic 파싱, 아래 3조건 모두 만족해야 통과
                     · 형태소가 정확히 1개 (조사·어미 결합 배제)
                     · 품사가 NNG(일반명사) 또는 NNP(고유명사)
                     · reading == 표면형   ← 미등록어(UNKNOWN) 추정 항목 제거
                     → 83,152개
④ 부적절어 차단      욕설·혐오·성적 표현 블록리스트 제거 → 83,091개
⑤ 정답 풀 선별       일반명사(NNG) 빈도 7,500위 이내
                     고유명사(NNP) 빈도 2,500위 이내  ← 인명·외래 표기 잡음 차단
                     + 손으로 고른 일상 구체명사(냉면·참외·우엉 …)
                     + 기존 큐레이션 195개, 관측된 잡음 블록리스트 제거
                     → 5,798개  (4칸 1,152 / 5칸 2,530 / 6칸 1,919 / 7칸 197)
```

**③의 `reading == 표면형` 조건이 핵심입니다.** MeCab은 모르는 글자도 통계로 추정해 NNG를 붙이기 때문에,
이 조건이 없으면 '즑쥕' 같은 비단어가 사전에 섞여 들어갑니다.

### 정답 풀에 빈도 컷만 쓰면 안 되는 이유

웹 코퍼스 빈도는 **추상어에 유리하고 구체명사에 불리**합니다.

| 단어 | 2음절 빈도 순위 | 일상 체감 |
|---|---|---|
| 참외 | 15,668위 | 누구나 아는 과일 |
| 우엉 | 15,985위 | 반찬으로 흔함 |
| 헌혈 | 9,000위 | 상대적으로 덜 씀 |

그래서 빈도 컷(일반명사 7,500위) 위에 **일상 구체명사 화이트리스트**를 얹었습니다.
단, 화이트리스트는 **"승격"만 담당**하고 사전에 없는 말을 새로 만들지 않습니다
(예: '깻잎'은 자모 8개라 범위 밖이므로 제외됨). 이 원칙이 없으면 '콩나', '팥빙' 같은
잘린 조각이 정답으로 들어갑니다.

반대 방향의 함정도 있습니다. 고유명사는 빈도만 보면 '럭키'(4,604위), '아메'(6,946위) 같은
**인명 조각**이 정답으로 올라옵니다. 그래서 NNP는 컷을 2,500위로 따로 잡았습니다 —
서울(52위)·부산(226위)·한강(2,219위)은 남고 잡음은 걸러집니다.

### 자모 범위를 4~7로 넓힌 이유

쌍자음·복합모음이 들어가면 2음절인데도 자모가 훅 늘어납니다.

```
땅콩 → ㄷㄷㅏㅇㅋㅗㅇ (7)     식빵 → ㅅㅣㄱㅂㅂㅏㅇ (7)
벚꽃 → ㅂㅓㅈㄱㄱㅗㅊ (7)     병원 → ㅂㅕㅇㅇㅜㅓㄴ (7)
```

4~6으로 자르면 이런 일상 단어가 통째로 빠집니다. 7칸까지 허용하되,
모바일에서 가로로 넘치지 않도록 타일 크기를 `min(52px, (화면폭 - 여백) / 칸수)` 로 계산합니다.

---

## 📱 Step 3. 화면 목록

| # | 화면 | 설명 | MVP |
|---|---|---|---|
| 1 | 게임 보드 | 6행 × N열(N=정답 자모 수, **4~7**). 칸 수에 맞춰 타일 크기 자동 축소 | ✅ |
| 2 | 자모 키보드 | 두벌식 배열 28키 + 입력/지움. 상태색 반영 | ✅ |
| 3 | 게임 방법 | 첫 방문 자동 노출. 색상 예시 포함 | ✅ |
| 4 | 통계 | 플레이/승률/연승/시도 분포 + 결과 공유 | ✅ |
| 5 | 설정 | 모드(오늘의 단어/무한), 칸 수(4~7/랜덤), 사전 검사, 하드 모드 | ✅ |
| 6 | 랭킹·프로필 | 친구 대결, 주간 랭킹 | 2차 |
| 7 | 단어 사전 | 지나간 정답 아카이브 + 뜻풀이 | 2차 |

### UI 특이점 — 조합 미리보기
자모만 보이면 무슨 단어를 치고 있는지 인지 부하가 큽니다.
보드 바로 아래에 **`ㅅㅐㅂ` → `샙`** 처럼 실시간 조합 결과를 띄우는 것이 체감 완성도를 가장 크게 올립니다.

---

## 🗃 Step 4. 데이터 모델 (2차 서버 도입 시)

**words**

| 컬럼 | 타입 | 설명 | 제약 |
|---|---|---|---|
| id | UUID | 기본키 | PK |
| word | TEXT | 단어 (예: 사과) | UNIQUE |
| jamo_key | TEXT | 분해 결과 (ㅅㅏㄱㅗㅏ) | INDEX |
| jamo_len | INT | 자모 수 (4~7) | INDEX |
| is_answer | BOOL | 정답 풀 포함 여부 | |
| difficulty | INT | 1~3 티어 | |

**daily_puzzles**

| 컬럼 | 타입 | 설명 | 제약 |
|---|---|---|---|
| play_date | DATE | 날짜(KST) | PK |
| word_id | UUID | 그날의 정답 | FK → words.id |
| jamo_len | INT | 칸 수 | |

**play_results**

| 컬럼 | 타입 | 설명 | 제약 |
|---|---|---|---|
| id | UUID | 기본키 | PK |
| user_id | UUID | 사용자 | FK |
| play_date | DATE | 날짜 | FK |
| tries | INT | 시도 횟수(실패=0) | |
| pattern | TEXT | 🟩🟨⬛ 패턴 | |

**관계**: `words` 1 : N `daily_puzzles` / `users` 1 : N `play_results` / `daily_puzzles` 1 : N `play_results`

> MVP는 이 테이블이 전부 **클라이언트 상수 배열**로 대체됩니다. 서버는 랭킹이 필요해질 때 붙이면 됩니다.

---

## 🔌 Step 5. API 명세 (2차)

**GET /api/daily**
- 설명: 오늘의 퍼즐 정보. 정답 단어는 절대 내려주지 않음
- 응답: `{ "date": "2026-08-06", "jamoLen": 5, "puzzleNo": 214 }`

**POST /api/guess**
- 설명: 서버 판정. 클라이언트에 정답을 두지 않아 치팅 방지
- 요청: `{ "date": "2026-08-06", "jamo": ["ㅅ","ㅏ","ㄱ","ㅗ","ㅏ"] }`
- 응답: `{ "valid": true, "result": ["correct","present","absent","absent","correct"], "solved": false }`

**GET /api/dict/check?word=사과**
- 응답: `{ "exists": true, "jamoLen": 5 }`

**POST /api/results**
- 요청: `{ "date": "2026-08-06", "tries": 4, "pattern": "⬛🟨...' }`
- 응답: `{ "rank": 1287, "percentile": 62.4 }`

---

## 🗓 Step 6. 개발 로드맵

### Sprint 1 (3~4일) — 엔진 ✅ *본 MVP에서 완료*
- [x] 자모 분해 / 조합(오토마타) / 판정 함수
- [x] 완성형 11,172자 라운드트립 테스트
- [x] 단어 데이터 구축: 허용 83,091개 / 정답 풀 5,798개

### Sprint 2 (1주) — 플레이 가능한 게임 ✅ *본 MVP에서 완료*
- [x] 보드·자모 키보드·애니메이션
- [x] 조합 미리보기, 사전 검사, 하드 모드
- [x] 통계 / 이모지 결과 공유 / 다크모드

### Sprint 3 (1주) — 콘텐츠 & 배포
- [x] ~~단어 확장~~ — mecab-ko-dic + wordfreq 파이프라인으로 완료
- [ ] 정답 풀 수동 검수 (고유명사·외래어 오탐 제거)
- [ ] 단어 난이도 티어링 (빈도 기반 3단계)
- [ ] localStorage 연동으로 통계·진행 상황 유지
- [ ] PWA 전환 (오프라인 플레이, 홈 화면 추가)
- [ ] Vercel 배포 + OG 이미지

### Sprint 4 (1~2주) — 서버 & 소셜
- [ ] Supabase 스키마 + 일일 퍼즐 배치
- [ ] 서버 판정 API로 전환 (치팅 방지)
- [ ] 주간 랭킹, 친구 결과 비교
- [ ] 카카오톡 공유 SDK 연동

---

## ✅ 검증 결과

| 항목 | 결과 |
|---|---|
| 완성형 음절 11,172자 분해→조합 라운드트립 | **실패 0건** |
| 2음절 조합 10,268건 라운드트립 | 중의성 225건(2.19%) → **사전 인덱스로 100% 해소** |
| 판정 로직 (중복 자모 포함 케이스) | 전 케이스 통과 |
| 단어 인정 여부 (한강·국장·냉면·벚꽃·땅콩·식빵·곶감·미역·참외·우엉) | **전부 인정** |
| 비단어 거부 ('즑쥕', 3음절 '컴퓨터') | 정상 거부 |
| 사전 인덱스 83,091개 구축 시간 | 63ms (초기 로딩 체감 없음) |
| 파일 크기 | 199KB (단어를 2글자씩 이어붙여 저장, JSON 대비 66% 절감) |
| 7칸 보드 모바일 레이아웃 (430px) | 보드 폭 394px, 넘침 없음 |
| 브라우저 E2E (입력→판정→승리→공유) | 통과, 콘솔 에러 없음 |

---

## 📌 확장 시 챙길 것

1. **정답 풀 검수** — 5,798개는 자동 생성이라 '디아', '마미' 같은 외래 표기 조각이 아직 소수 남아 있습니다. 운영한다면 정답 풀만 사람이 한 번 훑고, 매일 나갈 단어는 미리 큐를 짜두는 편이 안전합니다.
2. **표준어/외래어 정책** — '커피'는 허용, '컴퓨터'는 3음절이라 제외. 정답 풀에서는 인명·지명 계열 고유명사를 상위 2,500위로 제한했습니다.
3. **접근성** — 색맹 모드(초록/노랑 → 파랑/주황) 옵션을 Sprint 3에 넣으세요. 워들에서 가장 많이 요청받은 기능입니다.
4. **모바일 IME** — 화면 자모 키보드를 직접 그렸기 때문에 OS 한글 키보드가 뜨지 않습니다. `<input>`을 쓰지 않는 이 구조를 유지하세요.
